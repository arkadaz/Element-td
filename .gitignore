/target
/dist
/dist-release/

# Warcraft III maps kept alongside for reference. Not ours to redistribute.
*.w3x
*.w3m

# ...and the unpacked contents of one, for the same reason. `tools/extract.py`
# recreates this directory from a map you supply yourself.
/tools/greentd/

# Download caches for the asset bake. `tools/bake_models.py` and
# `tools/bake_textures.py` refetch these; `assets/*.bin` is the output that
# matters and is committed.
/tools/glb/
/tools/tex/
/tools/model_candidates.json
/tools/kenney_td/
/tools/kenney_tower-defense-kit.zip
/tools/kenney_nature/
/tools/kenney_nature-kit.zip
/tools/quaternius_rts/
/tools/quaternius_turrets/
__pycache__/

# Disposable visual-review captures are regenerated by the screenshot tests
# and should never pile up in a release worktree.
/shots/
/shots_*/
