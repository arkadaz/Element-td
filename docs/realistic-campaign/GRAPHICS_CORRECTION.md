# Graphics delivery rejected: 2026-09-11

The user inspected the live game and explicitly rejected its graphical quality. See `rejected-runtime-20260911.png`. This is the current failure reference; the three concept PNGs remain targets, not implementation evidence. Astra remains the designer and Terra remains the coder.

## Verified delivery gap

`IMPLEMENTATION_STATUS.md` confirms only a new ground albedo layer, retained stylized meshes and unconnected campaign data. The real-browser smoke did not finish. The image shows flat olive ground, a mustard ribbon road, blocky foliage, large hard perimeter slabs, blue/gray empty surround, tiny battlefield objects and bright repeated cyan build marks. This fails the agreed realism direction. Passing data/texture-contract tests cannot approve this image.

The current source still caps scene DPR at 1, uses the 920-point board cap, blends ground albedo at 0.24, uses a neutral normal fallback for the added moss layer, and draws existing imported/procedural geometry. These are investigation locations, not a prescription to max every value. Geometry/material/lighting changes are required together; sharpening, extra texture grain or a new backdrop alone will fail again.

## Revised execution order

**Latest platform requirement: browser-first Rust/WebAssembly.** Keep Rust as the game/simulation implementation and compile the playable application to `wasm32-unknown-unknown`. Retain the existing wgpu/egui and Trunk web pipeline. WebGPU is the preferred graphics backend; retain and verify the existing WebGL2 compatibility path, adapting shaders, asset formats and quality features to supported capabilities. Do not replace the deliverable with a native-only application, video, generated screenshot, or unrelated JavaScript game. Native builds are secondary and cannot substitute for browser acceptance.

The final browser deliverable must run from a normal local HTTP server and deployable static build, with no Rust installation or desktop installer required by players. Check asset loading paths, WASM MIME type, startup/loading/error UI, browser audio unlock, browser persistence, touch/pointer controls and fullscreen fallback. Verify real Chromium/Edge output and the available Firefox/Safari/mobile browser paths; report untested or unsupported combinations honestly. Where WebGPU is unavailable, the WebGL2 path must produce playable graphics at a suitable preset or display a clear compatibility explanation if a specific browser lacks a required capability. Never leave a black canvas or silently substitute the old unfinished game. Runtime build identification must make it clear which WASM/assets are actually served.

**Latest user requirement: fullscreen and responsive on all devices.** This is a P0 delivery requirement alongside graphics. The application must occupy the whole available browser viewport, with no fixed-size centered application, accidental page margins, scrollbars, stale canvas dimensions or 920-point maximum shrinking it. Support a deliberate Full screen control where browser APIs permit it, with an exit affordance and normal viewport fallback where unsupported. Browser fullscreen requires the user's gesture; do not claim it can be forced silently on every device.

Keep the square gameplay world undistorted and fully visible, while the surrounding application/render viewport adapts to the device aspect ratio. Maximize usable board size after actual controls and safe-area insets; the old fixed centered desktop-stage composition is superseded wherever it prevents using the screen. Do not promise an undistorted square can itself cover a rectangular display without cropping: use responsive control placement rather than stretching or hiding the board. Any unavoidable aspect-ratio surplus must be deliberate and quiet, not the current blue/gray missing-world gulf. Do not fill the screen with useless decoration as a substitute for a larger board.

Desktop landscape: board uses remaining space beside a restrained rail; essential HUD stays close to play. Tablet/portrait: full-width board with a compact resource strip and bottom action drawer. Short landscape phones: shallow controls, a collapsible roster and contextual selection sheet; no permanently tall bottom bar consuming most of the battle. Touch targets at least 44 CSS pixels, safe-area support, selected action always visible, and no hover-only essential information. Closing a drawer restores the maximum battle area. Pointer/touch events on overlays must never place towers behind them.

Use actual viewport/container resize and device-pixel-ratio changes for render targets and picking; distinguish CSS dimensions from render resolution. Fit on orientation changes, browser chrome expansion/collapse, fullscreen entry/exit, display changes and UI scaling without reloading or resetting play. Backgrounding/resuming must retain state. No page scroll during ordinary gameplay.

Required layout evidence: 360x800, 390x844, 768x1024, 1024x768, 844x390, 1280x720, 1366x768, 1920x1080, 2560x1440 and 3440x1440; include DPR 1/2 and desktop 125%/150% scaling. Check full board visibility, useful viewport occupancy, no clipped controls, no horizontal overflow, correct edge-tile picking, drawer input isolation, rotation and fullscreen exit. State any unsupported browser capabilities honestly; viewport fill remains mandatory even if native fullscreen is unavailable.

Suspend further campaign-table expansion until a materially convincing actual gameplay scene exists. Preserve all campaign requirements and completed work; five-hour integration follows the graphics milestone. Resume implementation rather than producing another foundation-only final answer.

1. Reproduce the user's current view on the actual served build. Record build fingerprint, served directory, URL, browser viewport, render-buffer size and quality preset. Verify source changes reach that build and report any stale server separately from missing implementation. Save the exact baseline.
2. Finish the asset production capability. Locate or install appropriate free authoring/conversion tooling using the normal permission flow, or use a working original mesh-generation pipeline. `blender` absent from PATH alone is not proof that realistic asset creation is impossible. Do not buy packs, bypass approvals or substitute cartoon assets. If a needed capability is truly blocked, state the exact operation/error and complete independent rendering work.
3. Replace the terrain surface pipeline and road presentation: plausible natural base color, real or properly authored normal/roughness detail, world-scale mapping, soft irregular moss-to-dirt borders following the real path. Keep route boundaries readable and legal picking deterministic. Remove raised rectangular road-edge treatment. Avoid painted-in sunlight/shadows in albedo.
4. Replace chunky perimeter rails and unrelated low-poly trees with believable stone/wood/vegetation, restricted to noninteractive edges. Tighten the camera to the actual terrain and tallest objects; remove the blue/gray empty gulf. Retain one fully visible fixed square board and attached rail. No scrolling.
5. Produce real meshes for the Seed ballista and Siege mortar plus one humanoid and one armored enemy, with believable materials, animation, muzzle alignment, recoil and impact. Use `tower-material-studies.png` and `creature-material-studies.png`; do not paste their images into the battlefield. Build a mixed, visibly active scene using those assets and compare actual output to the references.
6. In the actual rendered result, reduce armed legal-placement marks to quiet corner hints, with one hovered target strong. An active placement should not turn all grass into cyan graph paper. Preserve valid/invalid feedback, exact footprints, repeat building and cancel.
7. Fix resolution/anti-aliasing based on evidence. Expose quality-appropriate render scale, check devicePixelRatio and CSS/canvas scaling; high-quality desktop should not blindly be forced to one rendered pixel per UI point. Measure performance before increasing costs. Stable silhouettes matter more than detail visible only in huge screenshots.
8. Once this actual visual milestone works, propagate the production quality across the entire live asset inventory, including tower stages, enemies, props, icons, title and all quality modes. Then complete the live campaign/menu/save/spawn integration. A two-tower slice does not fulfill the original task by itself.

## Required evidence before calling graphics improved

- Actual browser captures at the same viewport: empty board, armed placement, at least 12 built towers, and an active 150+ enemy mixed horde. Also a 350/700-body stress capture. Use bounded deterministic fixtures rather than a capture loop that can hang on preparation or a dead run.
- Neutral-lit close-up renders of new tower/enemy meshes, plus their genuine tactical-scale appearance. Demonstrate separate wood, stone, metal, hide/chitin materials; include animation evidence.
- A clear screenshot comparison covering ground relief, natural route edges, foliage, border/crop, real model silhouettes and unobtrusive placement. Record what still fails; no binary-contract-only visual approval.
- Live browser console clean of rendering failures; verified clicks/build/cancel; complete route and outer sockets visible; measured frame pacing on named hardware. A responding HTTP server is not a successful game-render test.

Do not serve the largely unchanged Legacy picture as the completed redesign. The previous attempt honestly listed missing work, but listing it is not completion. Continue until the requested implementation is achieved or a specific external dependency truly requires user action, then report that dependency plainly.
